apply plugin: "war"

bootWar {
    mainClassName = "com.swecommerce.promotions.SwecommercePromotionsApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
